pipeline {
    agent any 
    stages {
        stage('git') {
            steps {
                git credentialsId: 'github', url: 'https://github.com/vigneshbalaji2262/bicsazurewp.git'd!' 
            }
        }
    }
}
